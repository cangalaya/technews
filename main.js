document.querySelector(".btn-menu").addEventListener('click', () => {       // seleccionamos el elemento
    document.querySelector(".nav-menu").classList.toggle("show");   //añadimos la clase en el elemento o la quitamos si la tiene con toggle
})